{% block form_row %}
    <div class="form-group my-3">
        {{ form_label(form) }}
        {{ form_widget(form, {attr : {'class' : 'form-control px-2'}}) }}
        {{ form_errors(form) }}
    </div>
{% endblock %}
