{% set title = "Ajouté une catégorie" %}

{% extends 'layout/base.html.twig' %}
{% block body %}
    <h1>{{ title }}</h1>

    {{ form_start(form) }}
    {{ form_row(form._token) }}
    {{ form_row(form.name) }}
    {{ form_row(form.create) }}
    {{ form_end(form) }}
{% endblock %}


