{% set title = "Catégorie" %}

{% extends 'layout/base.html.twig' %}
{% block body %}
    <div class="listContainer">
        <div class="container">
            <div class="name">{{ category.name }}</div>
        </div>
        <div onclick="location='{{ path('category_delete', {id: category.id}) }}'">Delete</div>
    </div>
{% endblock %}


