<div id="nav">
    <div id="navTitleContainer">
        <div id="navTitle">
            <h1>{{ app_name }}</h1>
        </div>
    </div>
    <ul id="navLink">
        {% if not app.user %}
            <li class="link"><a href="{{ path('app_login') }}">Connexion</a></li>
        {% else %}
            <li class="link"><a href="{{ path('category_index') }}">Catégorie</a></li>
            <li class="link"><a href="{{ path('place_index') }}">Place</a></li>
            <li class="link"><a href="{{ path('itinerary_index') }}">Itinéraire</a></li>
            {% if is_granted('ROLE_CHIEF') %}
            <li class="link"><a href="{{ path('manageEmployee_index') }}">User</a></li>
            {% endif %}
            <li class="link"><a href="{{ path('app_logout') }}">Déconnexion</a></li>
        {% endif %}
    </ul>
</div>
